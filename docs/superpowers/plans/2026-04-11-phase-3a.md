# Phase 3A Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deliver UI readability fixes, a home page navigation hub, and a working account creation screen — all without schema changes or new dependencies.

**Architecture:** All changes are additive and isolated. The two UI fixes are pure component edits. The home page replaces a 5-line redirect. Account creation adds one new page and one new API handler to an existing route file. Nothing existing is refactored.

**Tech Stack:** Next.js 14 App Router, TypeScript, Prisma 5 + Neon PostgreSQL, Zod, Tailwind CSS, Radix UI, Arabic RTL

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| `frontend/src/components/accounts/AccountTransactionsTable.tsx` | Modify | Add red/green directional coloring to amount and totalAmount columns |
| `frontend/src/components/accounts/AccountsTable.tsx` | Modify | Improve contrast on accountNumber and currency text |
| `frontend/src/app/page.tsx` | Modify | Replace redirect with full navigation hub page |
| `frontend/src/app/api/accounts/route.ts` | Modify | Add POST handler for account creation |
| `frontend/src/app/accounts/create/page.tsx` | Create | Account creation form (client component) |

**All paths are relative to:** `money-transfer-mvp/`

---

## Task 1: Transaction Direction Colors

**Files:**
- Modify: `frontend/src/components/accounts/AccountTransactionsTable.tsx`

The `amount` column currently renders white text regardless of direction. The `totalAmount` column is always gold. Fix both to be red for outgoing and green for incoming, and add a +/− prefix to the amount.

- [ ] **Step 1: Open the file and locate the two amount cells**

Read `frontend/src/components/accounts/AccountTransactionsTable.tsx`.

Find the `amount` cell at line ~189 and `totalAmount` cell at line ~196.

- [ ] **Step 2: Replace the amount cell**

Find:
```tsx
                <td className="px-4 py-3 tabular-nums font-medium text-foreground whitespace-nowrap">
                  {formatCurrency(tx.amount, tx.currency as Currency)}
                </td>
```

Replace with:
```tsx
                <td
                  className="px-4 py-3 tabular-nums font-semibold whitespace-nowrap"
                  style={{ color: tx.direction === 'outgoing' ? '#f87171' : '#4ade80' }}
                >
                  {tx.direction === 'outgoing' ? '−' : '+'}{formatCurrency(tx.amount, tx.currency as Currency)}
                </td>
```

- [ ] **Step 3: Replace the totalAmount cell**

Find:
```tsx
                <td
                  className="px-4 py-3 tabular-nums font-semibold whitespace-nowrap"
                  style={{ color: '#c9a84c' }}
                >
                  {formatCurrency(tx.totalAmount, tx.currency as Currency)}
                </td>
```

Replace with:
```tsx
                <td
                  className="px-4 py-3 tabular-nums font-semibold whitespace-nowrap"
                  style={{ color: tx.direction === 'outgoing' ? '#f87171' : '#4ade80' }}
                >
                  {formatCurrency(tx.totalAmount, tx.currency as Currency)}
                </td>
```

- [ ] **Step 4: Verify visually**

Navigate to `/accounts/[id]` for an account that has both incoming and outgoing transactions. Confirm:
- Outgoing rows show red amounts with a `−` prefix
- Incoming rows show green amounts with a `+` prefix
- Direction badge colours are unchanged

- [ ] **Step 5: Commit**

```bash
cd money-transfer-mvp/frontend
git add src/components/accounts/AccountTransactionsTable.tsx
git commit -m "fix: color transaction amounts red/green by direction"
```

---

## Task 2: Accounts Table Readability

**Files:**
- Modify: `frontend/src/components/accounts/AccountsTable.tsx`

`accountNumber` and both currency text elements use `text-muted-foreground` which is too dim against the dark background.

- [ ] **Step 1: Open the file**

Read `frontend/src/components/accounts/AccountsTable.tsx`.

- [ ] **Step 2: Fix the accountNumber cell**

Find:
```tsx
              <td
                className="px-4 py-3 font-mono text-sm text-muted-foreground"
                dir="ltr"
              >
                {account.accountNumber}
              </td>
```

Replace with:
```tsx
              <td
                className="px-4 py-3 font-mono text-sm"
                style={{ color: 'rgba(255,255,255,0.75)' }}
                dir="ltr"
              >
                {account.accountNumber}
              </td>
```

- [ ] **Step 3: Fix the currency cell**

Find:
```tsx
              <td className="px-4 py-3 text-muted-foreground">
                <span className="font-mono text-xs font-bold">
                  {account.currency}
                </span>
                <span className="text-xs mr-1">
                  · {CURRENCY_LABELS[account.currency as Currency]}
                </span>
              </td>
```

Replace with:
```tsx
              <td className="px-4 py-3">
                <span
                  className="font-mono text-xs font-bold"
                  style={{ color: 'rgba(255,255,255,0.80)' }}
                >
                  {account.currency}
                </span>
                <span
                  className="text-xs mr-1"
                  style={{ color: 'rgba(255,255,255,0.55)' }}
                >
                  · {CURRENCY_LABELS[account.currency as Currency]}
                </span>
              </td>
```

- [ ] **Step 4: Verify visually**

Navigate to `/accounts`. Confirm:
- Account names are white (unchanged)
- Account numbers are clearly readable (light grey, not dimmed out)
- Currency code (e.g. "SAR") is clearly readable
- Currency label (e.g. "ريال سعودي") is readable but secondary
- Balance remains gold (unchanged)

- [ ] **Step 5: Commit**

```bash
git add src/components/accounts/AccountsTable.tsx
git commit -m "fix: improve accounts table text contrast for readability"
```

---

## Task 3: Home Page Navigation Hub

**Files:**
- Modify: `frontend/src/app/page.tsx`

Replace the current 5-line redirect with a full navigation hub page that matches the existing dark gold theme.

- [ ] **Step 1: Replace page.tsx entirely**

Overwrite `frontend/src/app/page.tsx` with:

```tsx
import Link from 'next/link';

export default function HomePage() {
  return (
    <main className="min-h-screen bg-background">
      {/* Header */}
      <header className="border-b border-border bg-card sticky top-0 z-10">
        <div className="max-w-5xl mx-auto px-4 py-4 flex items-center justify-between">
          <div className="flex items-center gap-3">
            <div
              className="flex h-9 w-9 items-center justify-center rounded-lg font-bold text-sm"
              style={{
                background: 'linear-gradient(135deg, #c9a84c, #f0c040)',
                color: '#0a0f1e',
              }}
            >
              ت
            </div>
            <div>
              <h1 className="font-bold text-foreground text-lg leading-none">
                نظام التحويلات
              </h1>
              <p className="text-xs text-muted-foreground mt-0.5">
                Money Transfer System
              </p>
            </div>
          </div>
          <span
            className="inline-flex items-center gap-1.5 text-xs px-2.5 py-1 rounded-full font-medium"
            style={{
              border: '1px solid #c9a84c',
              color: '#c9a84c',
              background: 'rgba(201,168,76,0.08)',
            }}
          >
            <span
              className="h-1.5 w-1.5 rounded-full inline-block animate-pulse"
              style={{ background: '#c9a84c' }}
            />
            متصل
          </span>
        </div>
      </header>

      {/* Hero */}
      <div className="max-w-5xl mx-auto px-4 pt-14 pb-10 text-center">
        <h2 className="text-3xl font-extrabold text-foreground mb-3">
          نظام التحويلات
        </h2>
        <p className="text-muted-foreground text-base max-w-xl mx-auto">
          إدارة الحسابات والتحويلات ومتابعة الحركة المالية بسهولة
        </p>
      </div>

      {/* Navigation Cards */}
      <div className="max-w-5xl mx-auto px-4 pb-16">
        <div className="grid grid-cols-1 sm:grid-cols-3 gap-4">
          {/* Transfer */}
          <Link
            href="/transfer"
            className="group flex flex-col gap-3 p-6 rounded-xl border transition-all hover:scale-[1.02]"
            style={{
              background: 'hsl(224 44% 9%)',
              borderColor: 'hsl(221 42% 17%)',
            }}
          >
            <div
              className="flex h-11 w-11 items-center justify-center rounded-lg text-xl"
              style={{
                background: 'rgba(201,168,76,0.12)',
                border: '1px solid rgba(201,168,76,0.25)',
              }}
            >
              ↔
            </div>
            <div>
              <p className="font-bold text-foreground text-base mb-1">
                إجراء تحويل
              </p>
              <p className="text-sm" style={{ color: 'rgba(255,255,255,0.5)' }}>
                تحويل الأموال بين الحسابات
              </p>
            </div>
            <span
              className="mt-auto text-xs font-semibold"
              style={{ color: '#c9a84c' }}
            >
              الانتقال ←
            </span>
          </Link>

          {/* Accounts */}
          <Link
            href="/accounts"
            className="group flex flex-col gap-3 p-6 rounded-xl border transition-all hover:scale-[1.02]"
            style={{
              background: 'hsl(224 44% 9%)',
              borderColor: 'hsl(221 42% 17%)',
            }}
          >
            <div
              className="flex h-11 w-11 items-center justify-center rounded-lg text-xl"
              style={{
                background: 'rgba(201,168,76,0.12)',
                border: '1px solid rgba(201,168,76,0.25)',
              }}
            >
              ☰
            </div>
            <div>
              <p className="font-bold text-foreground text-base mb-1">
                عرض الحسابات
              </p>
              <p className="text-sm" style={{ color: 'rgba(255,255,255,0.5)' }}>
                قائمة الحسابات وأرصدتها
              </p>
            </div>
            <span
              className="mt-auto text-xs font-semibold"
              style={{ color: '#c9a84c' }}
            >
              الانتقال ←
            </span>
          </Link>

          {/* Create Account */}
          <Link
            href="/accounts/create"
            className="group flex flex-col gap-3 p-6 rounded-xl border transition-all hover:scale-[1.02]"
            style={{
              background: 'hsl(224 44% 9%)',
              borderColor: 'hsl(221 42% 17%)',
            }}
          >
            <div
              className="flex h-11 w-11 items-center justify-center rounded-lg text-xl"
              style={{
                background: 'rgba(201,168,76,0.12)',
                border: '1px solid rgba(201,168,76,0.25)',
              }}
            >
              +
            </div>
            <div>
              <p className="font-bold text-foreground text-base mb-1">
                إنشاء حساب جديد
              </p>
              <p className="text-sm" style={{ color: 'rgba(255,255,255,0.5)' }}>
                إضافة حساب للنظام
              </p>
            </div>
            <span
              className="mt-auto text-xs font-semibold"
              style={{ color: '#c9a84c' }}
            >
              الانتقال ←
            </span>
          </Link>
        </div>
      </div>

      {/* Footer */}
      <footer className="border-t border-border">
        <div className="max-w-5xl mx-auto px-4 py-4 text-center">
          <p className="text-xs text-muted-foreground">
            © 2026 نظام تحويل الأموال · جميع الحقوق محفوظة
          </p>
        </div>
      </footer>
    </main>
  );
}
```

- [ ] **Step 2: Verify visually**

Navigate to `/`. Confirm:
- Three navigation cards are visible
- All three links navigate correctly (`/transfer`, `/accounts`, `/accounts/create`)
- `/accounts/create` returns a 404 for now — that's expected until Task 5
- Theme matches existing pages (dark background, gold accents)
- Existing `/transfer` and `/accounts` pages still load correctly

- [ ] **Step 3: Commit**

```bash
git add src/app/page.tsx
git commit -m "feat: add home page navigation hub"
```

---

## Task 4: POST /api/accounts Handler

**Files:**
- Modify: `frontend/src/app/api/accounts/route.ts`

Add a `POST` handler to the existing route file. The `GET` handler already exists and must not be changed.

- [ ] **Step 1: Open the file**

Read `frontend/src/app/api/accounts/route.ts`.

Current content is ~27 lines with only a `GET` export.

- [ ] **Step 2: Replace the file with GET + POST**

Overwrite `frontend/src/app/api/accounts/route.ts` with:

```ts
import { NextRequest, NextResponse } from 'next/server';
import { prisma } from '@/lib/prisma';
import { z } from 'zod';

export async function GET() {
  try {
    const accounts = await prisma.account.findMany({
      orderBy: { holderName: 'asc' },
    });

    const data = accounts.map((account) => ({
      id: account.id,
      accountNumber: account.accountNumber,
      holderName: account.holderName,
      balance: account.balance.toNumber(),
      currency: account.currency,
      createdAt: account.createdAt.toISOString(),
    }));

    return NextResponse.json({ success: true, data });
  } catch (error) {
    console.error('Error fetching accounts:', error);
    return NextResponse.json(
      { success: false, error: { message: 'فشل في جلب الحسابات' } },
      { status: 500 }
    );
  }
}

const createAccountSchema = z.object({
  holderName: z.string().min(1, 'اسم الحامل مطلوب'),
  accountNumber: z.string().min(1, 'رقم الحساب مطلوب'),
  balance: z
    .number({ invalid_type_error: 'الرصيد يجب أن يكون رقماً' })
    .min(0, 'الرصيد يجب أن يكون صفراً أو أكثر'),
  currency: z.enum(['SAR', 'USD', 'EUR', 'GBP', 'AED'], {
    errorMap: () => ({ message: 'العملة غير صالحة' }),
  }),
});

export async function POST(req: NextRequest) {
  try {
    const body = await req.json();
    const parsed = createAccountSchema.safeParse(body);

    if (!parsed.success) {
      const message = parsed.error.errors.map((e) => e.message).join(', ');
      return NextResponse.json(
        { success: false, error: { message } },
        { status: 422 }
      );
    }

    const { holderName, accountNumber, balance, currency } = parsed.data;

    const existing = await prisma.account.findUnique({
      where: { accountNumber },
    });

    if (existing) {
      return NextResponse.json(
        { success: false, error: { message: 'رقم الحساب مستخدم بالفعل' } },
        { status: 400 }
      );
    }

    const account = await prisma.account.create({
      data: {
        holderName,
        accountNumber,
        balance,
        currency,
      },
    });

    const data = {
      id: account.id,
      accountNumber: account.accountNumber,
      holderName: account.holderName,
      balance: account.balance.toNumber(),
      currency: account.currency,
      createdAt: account.createdAt.toISOString(),
    };

    return NextResponse.json({ success: true, data }, { status: 201 });
  } catch (error) {
    console.error('Error creating account:', error);
    return NextResponse.json(
      { success: false, error: { message: 'فشل في إنشاء الحساب' } },
      { status: 500 }
    );
  }
}
```

- [ ] **Step 3: Test GET still works**

Navigate to `/accounts`. The accounts list should load exactly as before. Confirm no regression.

- [ ] **Step 4: Test POST manually with curl (optional)**

```bash
curl -s -X POST http://localhost:3000/api/accounts \
  -H "Content-Type: application/json" \
  -d '{"holderName":"اختبار","accountNumber":"TEST-001","balance":5000,"currency":"SAR"}' | jq .
```

Expected output:
```json
{
  "success": true,
  "data": {
    "id": "...",
    "accountNumber": "TEST-001",
    "holderName": "اختبار",
    "balance": 5000,
    "currency": "SAR",
    "createdAt": "..."
  }
}
```

Run again with the same `accountNumber` — expect:
```json
{ "success": false, "error": { "message": "رقم الحساب مستخدم بالفعل" } }
```

- [ ] **Step 5: Commit**

```bash
git add src/app/api/accounts/route.ts
git commit -m "feat: add POST /api/accounts for account creation"
```

---

## Task 5: /accounts/create Page

**Files:**
- Create: `frontend/src/app/accounts/create/page.tsx`

A client component with a form that posts to `POST /api/accounts`. On success, redirects to `/accounts`. On error, displays the Arabic error message inline.

- [ ] **Step 1: Create the file**

Create `frontend/src/app/accounts/create/page.tsx` with:

```tsx
'use client';

import { useState } from 'react';
import { useRouter } from 'next/navigation';
import Link from 'next/link';

const CURRENCIES = [
  { value: 'SAR', label: 'ريال سعودي (SAR)' },
  { value: 'USD', label: 'دولار أمريكي (USD)' },
  { value: 'EUR', label: 'يورو (EUR)' },
  { value: 'GBP', label: 'جنيه إسترليني (GBP)' },
  { value: 'AED', label: 'درهم إماراتي (AED)' },
];

export default function CreateAccountPage() {
  const router = useRouter();

  const [holderName, setHolderName] = useState('');
  const [accountNumber, setAccountNumber] = useState('');
  const [balance, setBalance] = useState('');
  const [currency, setCurrency] = useState('SAR');
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    setError(null);
    setLoading(true);

    try {
      const res = await fetch('/api/accounts', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          holderName,
          accountNumber,
          balance: parseFloat(balance),
          currency,
        }),
      });

      const json = await res.json();

      if (!json.success) {
        setError(json.error?.message ?? 'فشل في إنشاء الحساب');
        return;
      }

      router.push('/accounts');
    } catch {
      setError('حدث خطأ غير متوقع. يرجى المحاولة مرة أخرى.');
    } finally {
      setLoading(false);
    }
  }

  const inputStyle = {
    background: 'hsl(224 44% 9%)',
    border: '1px solid hsl(221 42% 17%)',
    color: 'hsl(var(--foreground))',
    borderRadius: '0.5rem',
    padding: '0.625rem 0.875rem',
    width: '100%',
    fontSize: '0.875rem',
    outline: 'none',
  } as React.CSSProperties;

  const labelStyle = {
    display: 'block',
    fontSize: '0.8125rem',
    fontWeight: 600,
    color: 'rgba(255,255,255,0.75)',
    marginBottom: '0.375rem',
  } as React.CSSProperties;

  return (
    <main className="min-h-screen bg-background">
      {/* Header */}
      <header className="border-b border-border bg-card sticky top-0 z-10">
        <div className="max-w-5xl mx-auto px-4 py-4 flex items-center justify-between">
          <div className="flex items-center gap-3">
            <div
              className="flex h-9 w-9 items-center justify-center rounded-lg font-bold text-sm"
              style={{
                background: 'linear-gradient(135deg, #c9a84c, #f0c040)',
                color: '#0a0f1e',
              }}
            >
              ت
            </div>
            <div>
              <h1 className="font-bold text-foreground text-lg leading-none">
                نظام التحويلات
              </h1>
              <p className="text-xs text-muted-foreground mt-0.5">
                Money Transfer System
              </p>
            </div>
          </div>
          <span
            className="inline-flex items-center gap-1.5 text-xs px-2.5 py-1 rounded-full font-medium"
            style={{
              border: '1px solid #c9a84c',
              color: '#c9a84c',
              background: 'rgba(201,168,76,0.08)',
            }}
          >
            <span
              className="h-1.5 w-1.5 rounded-full inline-block animate-pulse"
              style={{ background: '#c9a84c' }}
            />
            متصل
          </span>
        </div>
      </header>

      {/* Content */}
      <div className="max-w-5xl mx-auto px-4 py-10">
        {/* Back link */}
        <Link
          href="/accounts"
          className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground transition-colors mb-8"
        >
          ← الحسابات
        </Link>

        <div className="max-w-lg mx-auto">
          <div className="mb-8">
            <h2 className="text-2xl font-extrabold text-foreground mb-1">
              إنشاء حساب جديد
            </h2>
            <p className="text-sm text-muted-foreground">
              أدخل بيانات الحساب لإضافته إلى النظام
            </p>
          </div>

          {/* Form Card */}
          <div
            className="rounded-xl border p-6 space-y-5"
            style={{
              background: 'hsl(224 44% 9%)',
              borderColor: 'hsl(221 42% 17%)',
            }}
          >
            <form onSubmit={handleSubmit} className="space-y-5">
              {/* Holder Name */}
              <div>
                <label style={labelStyle}>اسم الحامل</label>
                <input
                  type="text"
                  value={holderName}
                  onChange={(e) => setHolderName(e.target.value)}
                  placeholder="مثال: أحمد محمد"
                  required
                  disabled={loading}
                  style={inputStyle}
                />
              </div>

              {/* Account Number */}
              <div>
                <label style={labelStyle}>رقم الحساب</label>
                <input
                  type="text"
                  value={accountNumber}
                  onChange={(e) => setAccountNumber(e.target.value)}
                  placeholder="مثال: ACC-001"
                  required
                  disabled={loading}
                  style={{ ...inputStyle, direction: 'ltr', textAlign: 'right' }}
                />
              </div>

              {/* Opening Balance */}
              <div>
                <label style={labelStyle}>الرصيد الافتتاحي</label>
                <input
                  type="number"
                  value={balance}
                  onChange={(e) => setBalance(e.target.value)}
                  placeholder="0.00"
                  min="0"
                  step="0.01"
                  required
                  disabled={loading}
                  style={{ ...inputStyle, direction: 'ltr', textAlign: 'right' }}
                />
              </div>

              {/* Currency */}
              <div>
                <label style={labelStyle}>العملة</label>
                <select
                  value={currency}
                  onChange={(e) => setCurrency(e.target.value)}
                  disabled={loading}
                  style={inputStyle}
                >
                  {CURRENCIES.map((c) => (
                    <option key={c.value} value={c.value}>
                      {c.label}
                    </option>
                  ))}
                </select>
              </div>

              {/* Error */}
              {error && (
                <div
                  className="rounded-lg px-4 py-3 text-sm font-medium"
                  style={{
                    background: 'rgba(239,68,68,0.1)',
                    border: '1px solid rgba(239,68,68,0.3)',
                    color: '#f87171',
                  }}
                >
                  {error}
                </div>
              )}

              {/* Submit */}
              <button
                type="submit"
                disabled={loading}
                className="w-full py-2.5 rounded-lg text-sm font-bold transition-opacity disabled:opacity-60"
                style={{
                  background: 'linear-gradient(135deg, #c9a84c, #f0c040)',
                  color: '#0a0f1e',
                }}
              >
                {loading ? 'جارٍ الإنشاء...' : 'إنشاء الحساب'}
              </button>
            </form>
          </div>
        </div>
      </div>

      {/* Footer */}
      <footer className="border-t border-border mt-16">
        <div className="max-w-5xl mx-auto px-4 py-4 text-center">
          <p className="text-xs text-muted-foreground">
            © 2026 نظام تحويل الأموال · جميع الحقوق محفوظة
          </p>
        </div>
      </footer>
    </main>
  );
}
```

- [ ] **Step 2: Verify form renders**

Navigate to `/accounts/create`. Confirm:
- Page loads with the correct header, back link, and form
- All four fields are visible with correct Arabic labels
- Currency dropdown shows all 5 options
- Submit button is gold and says "إنشاء الحساب"

- [ ] **Step 3: Test successful creation**

Fill in the form:
- اسم الحامل: `محمد علي`
- رقم الحساب: `TEST-NEW-001`
- الرصيد الافتتاحي: `10000`
- العملة: `SAR`

Submit. Confirm:
- Page redirects to `/accounts`
- The new account `محمد علي` appears in the list

- [ ] **Step 4: Test duplicate account number**

Go back to `/accounts/create` and submit with the same `accountNumber` (TEST-NEW-001). Confirm:
- An Arabic error message appears: "رقم الحساب مستخدم بالفعل"
- The form does NOT navigate away

- [ ] **Step 5: Test the new account in the transfer form**

Navigate to `/transfer`. Confirm the new account appears in both the "من" and "إلى" dropdowns.

- [ ] **Step 6: Test the new account statement**

Navigate to `/accounts`, click "عرض الكشف" on the new account. Confirm the account statement page loads and shows the correct balance.

- [ ] **Step 7: Commit**

```bash
git add src/app/accounts/create/page.tsx
git commit -m "feat: add /accounts/create page for manual account creation"
```

---

## Phase 3A Complete Verification

After all 5 tasks, verify nothing is broken:

- [ ] `/` loads the home page with 3 navigation cards
- [ ] `/transfer` loads and works (existing functionality unchanged)
- [ ] `/accounts` loads the accounts list with improved text contrast
- [ ] `/accounts/[id]` shows account statement with red/green transaction amounts
- [ ] `/accounts/create` creates a real account and redirects to `/accounts`
- [ ] The newly created account appears in `/transfer` dropdowns

---

## Not in Phase 3A

The following are deferred to Phase 3B and must NOT be touched:

- Prisma schema changes (mobile, address)
- SheetJS / Excel import-export
- Any changes to transfer logic
- Any changes to `/api/transfer`
